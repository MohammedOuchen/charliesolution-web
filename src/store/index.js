import { createStore } from 'vuex'
import tracker from './tracker/index'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tracker
  }
})
